import 'package:flutter/material.dart';

class Telemetics extends StatelessWidget {
  const Telemetics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("telemetics")),
      body: Text('gdfgfdg'),
    );
  }
}
